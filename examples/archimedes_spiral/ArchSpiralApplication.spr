uses 
  W3System, ArchSpiralApp;

var mApplication: TApplication;
try
  InitVendorInfo();
  mApplication := TApplication.Create;
  mApplication.RunApp;
except
  on e: exception do
    ShowMessage(e.message);
end;
